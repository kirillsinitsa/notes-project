<h3><%= @note.title %></h3>
<p><%= @note.body %></p>

<hr>

<%= link_to 'Edit', edit_note_path(@note), class: 'btn btn-outline-secondary' %>
<%= link_to 'Delete', note_path(@note), method: :delete,
 data: {confirm: "Do you want to delete this note?"}, class: 'btn btn-outline-danger' %>
