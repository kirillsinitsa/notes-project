<div align="center">
<h3> My Notes </h3>
</div>

<% @notes.each do |note| %>

<div class="alert alert-secondary">
  <h4><%= note.title %></h4>
  <p><%= note.body %></p>
  <%= link_to 'Read More', note_path(note), class: "btn btn-outline-info" %>
</div>

<% end %>
